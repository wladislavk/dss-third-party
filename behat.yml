default:
    suites:
        default:
            paths:
                - "%paths.base%/Features"
            contexts:
                - Contexts\Main
                - Contexts\Database
                - Contexts\Google
    extensions:
        Behat\MinkExtension:
            base_url: "http://example.com"
            sessions:
                default:
                    selenium2:
                        browser: phantomjs
                        wd_host: "http://localhost:8643/wd/hub"
